create procedure regdata(username varchar(100),password varchar(100),role varchar(100),name varchar(100),contact varchar(10),image varchar(100))
    begin
    declare uid int;
    insert into user values('0',username,password,contact,role);
    select last_insert_id() into uid;
    insert into admin values('0',name,username,image,uid);
    end //


